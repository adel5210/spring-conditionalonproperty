# Spring @ConditionOnProperty

Runs certain service with a passed cmd args
```
One way is thru application.properties
ex:- 
db.service.select=cache

Other way is thru command
-Ddb.service.select=cache
```